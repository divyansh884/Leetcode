class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // int cnto = 0, cnte = 0, n = nums.size();
        // for (int i = 0; i < nums1.size(); i++) {
        //     if (nums[i] % 2 == 0)
        //         cnte++;
        //     else
        //         cnto++;
        // }
        // if (cnte == n || cnto == n)
        return true;
    }
};